# Public URL used for canonical metadata and social previews.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Add payment, database, email, and auth provider keys here before enabling
# server-backed checkout. Never expose secret keys with NEXT_PUBLIC_.
